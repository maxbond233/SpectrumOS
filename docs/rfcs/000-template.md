# RFC-NNN: 标题

- 状态: 草案 / 讨论中 / 已接受 / 已实施 / 已废弃
- 作者:
- 创建日期:
- 目标版本:

## 动机

为什么需要这个变更？当前存在什么问题？

## 设计方案

### 数据模型变更

描述 `db/models.py` 的变更（如有）。

### Agent 行为变更

描述各 Agent 的行为变更（如有）。

### API 变更

描述新增或修改的 API 端点（如有）。

### 配置变更

描述 `config/settings.yaml` 的变更（如有）。

## 替代方案

考虑过哪些其他方案？为什么没有选择？

## 迁移计划

如何从当前状态迁移到新方案？是否需要数据迁移？

## 贯穿性约束检查

- [ ] 不违反「不删除」原则（归档而非硬删）
- [ ] 不违反「字段隔离」原则（AI 写 ai_notes/assigned_agent，人写 name/status/priority）
- [ ] 高风险操作设置 `review_needed=True`
- [ ] 所有写操作记录 Activity Log
- [ ] 新增字段 `nullable=True` 或有默认值（向后兼容）
- [ ] 功能可通过配置降级

## 未解决问题

列出尚未确定的设计决策。
